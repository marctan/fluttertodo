import 'package:flutter/material.dart';

enum TabView { all, complete, incomplete }
enum StatusVal { incomplete, complete }

class Constants {
  // ignore: constant_identifier_names
  static const PRIMARY_COLOR = Color(0xFF2F2798);
}
